# Backport.2 checkpoint

**Completed:** [clean Linux CI](https://github.com/AndSuch-Mods/General-AI-Test-Space-2/actions/runs/35496578548) passed and [the artifact](https://github.com/AndSuch-Mods/General-AI-Test-Space-2/actions/runs/35496578548/artifacts/10601041163) was downloaded and inspected. All 17 tests passed with zero failures/errors/skips; production JAR SHA-256 `57c6802d072f2bd513c91922f0d48c2056ee80dfff14f9feb64737d2e8252c99` equals the Windows build. Tested source commit: d6876cdac1769c060300d92fdcfb6415a58156c3. The workflow was triggered at 71aee8d and published its verified checksum inventory before testing; validation/ci-validation.json records both commits. Final handoff changes only documentation/reports and removal of the temporary publishing step, with [skip ci]. CI permissions are again contents:read. No remaining compilation or CI blocker.

Target: Minecraft 1.21.1 / Java 21, Sodium KIRFiWG4 (0.8.12+mc1.21.1), Iris bAo1Qhte (1.8.14-beta.1+mc1.21.1), Fabric API Mys3P7lK and optional Continuity kSPJ4hQv. Exact artifact manifests/hashes are saved. Original branch checkpoint 5f882c4 was current on fetch; no newer changes were discarded.

Completed locally:
- Preserved backport.1 source/history, original JAR/CI ZIP in .reference/checkpoint-backport1, and prior reports in validation/backport1.
- Inspected actual manifests, bundled libraries, dependency/break ranges and Java bytecode. Loader pin/minimum is now 0.18.4 to support requested Dynamic Trees (>=0.18.4) and Sodium Extra (>=0.18).
- Loom 1.10.5 rejected the selected Sodium JAR's newer Loom metadata. Pinned Loom 1.16.3/Gradle 9.4.0, based on its required Gradle API version; runtime Java remains 21. Static mixin-remap selectors are checked in the production JAR. Dependency verification stays enabled.
- Updated actual Sodium texture binding to its texture API/reserved-slot enum; confirmed Sodium and Iris 15-bit UV encoders and Iris gtextureSize supplier bytecode. All 24 required target methods and shadow descriptors resolve.
- Added concrete-sampler helper specialization, including Bliss POMSwitch and nested helpers, and legacy gradient/LOD handling. Non-atlas ocean/noise samplers stay separate. Existing aligned diffuse/normal-height/specular uploads, material mip/animation handling, allocation checks and reload cleanup retained. Synthetic maps now exercise emission and SSS too.
- Local Java 21 remapped JAR builds. **17 JUnit tests, zero failures/errors/skips**; original generator test passes. Actual pinned Bliss, Photon and BSL POM/material functions compile offline in minimal harnesses with glslang 16.6.0. Artifact validator passes Java 21, exact hard dependencies, license, required mixins and static intermediary selectors. See validation/local-validation.json.
- Loom emitted remap warnings while processing upstream dependency mixins; they were not suppressed. Actual application of those dependencies and this mod remains a runtime check.
- Historical failed checks: initial build blocked on old Loom; resolved by required tool upgrade. Photon fixture initially used a function instead of the source's rcp macro; corrected harness and passed. Previous artifact validator expected a refmap; updated to validate Loom's static mapped selectors. These are not hidden skipped tests.
- Optional artifact candidates and exact screenshot manifest mismatches are documented in COMPATIBILITY.md. Physics Pro v188b JAR was not found/provided; requested its path. EMF/ETF/ImmediatelyFast/ModernFix versions remain unlocked. FCAP required by Dynamic Trees remains an unselected optional dependency.

Source update published as 06a81b1, with checksum publication commit 36582b5. The connector encountered transport failures while publishing checksum payloads, so a temporary CI step publishes checksum-verified compressed inventory; it will be removed and permissions returned to contents:read. CI 35496257388 then exposed intermediary 1.21.1 JAR/POM hashes removed by the current-graph pruning (local Loom cache hid that resolution). Restoring the complete already-verified inventory, including those cold-cache dependencies, without relaxing verification. CI 35496381453 then exposed Loom 1.16 generated remap JAR timestamps under the new remapped.* coordinate groups. Exempted only those locally generated coordinates and removed their machine-specific hashes; original downloaded inputs remain checksum-verified. Final CI 35496578548 passed; no pending offline build blocker. Corrected inventory published by d6876cd. The temporary publication step has been removed and CI permissions restored to contents:read in the final handoff changes. No Minecraft launch, runtime Mixin application, GPU test, profile modification, paid input upload, full shader-pack compilation or modpack compatibility test was performed. Required manual checks are USER_TESTS.md. Four-page/sampler/VRAM limits and unhandled shader operations remain documented.

Resume: with Java 21 and GLSLANG_VALIDATOR pointing at glslang 16.6.0, run `python tools/fetch_shader_fixtures.py`, `./gradlew build`, `python tools/validate_artifact.py`, and `python tools/test_generator.py`. On this Windows host portable Java and glslang are in .reference/java and .reference/glslang/bin; JAVA_TOOL_OPTIONS=-Djavax.net.ssl.trustStoreType=Windows-ROOT uses Windows certificates without disabling TLS. Logs and inspected inputs remain in .reference/target2. Do not include that directory or build/shader-inputs in commits/artifacts.
